import { FormattedMessage } from "react-intl";

export const allDenomination = (
    <FormattedMessage
        id="homepage_volunteering_all"
        defaultMessage="Tutti"
        description="Reference to both events and organizations in the homepage"
    />
);

export const organizationDenomination = (
    <FormattedMessage
        id="homepage_volunteering_organizations"
        defaultMessage="Enti"
        description="Organizations reference in the homepage"
    />
);

export const eventDenomination = (
    <FormattedMessage
        id="homepage_volunteering_events"
        defaultMessage="Eventi"
        description="Events reference in the homepage"
    />
);
